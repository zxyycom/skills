---
title: 由 task-graph 承接可恢复的任务协调
status: archived
alignment: aligned
createdAt: 2026-08-07T07:19:01Z
purpose: 让 agent 持续维护仍有协调价值的任务、拓扑和执行边界，而不以存活时间判断任务是否有效。
background: 任务图可能需要跨越较长工作周期；把内容职责上的临时性误写成短生命周期，会诱导调用方按时间清理仍有价值的协调事实。
decision: 由 task-graph 承接当前工作中仍有协调价值的可恢复任务图；task entry 不承担长期知识，但任务可以保留到协调价值消失。
relations:
  - type: 修订
    target: task-graph/own-short-lived-task-orchestration.md
---

## 目的

- 让 agent 从权威任务图恢复当前仍需协调的候选、阻塞、父子、依赖、排斥和执行归属。
- 让任务是否保留取决于协调价值，而不是创建时间、经过时长或是否跨越多个上下文。
- 保持任务协调、持久 change 和代理编排各自拥有清楚的责任边界。

## 背景

- 线性计划不能稳定表达同时存在的候选任务、父子结构、前置依赖和并发排斥；只把关系留在对话中又会在上下文压缩或动态追加后丢失。
- 有些任务会很快完成，有些任务会跨越较长工作周期，但只要它们仍参与选择、阻塞、交接或执行协调，任务图就仍有保存价值。
- “临时”真正限制的是 task entry 的内容职责：它不应替代长期需求、设计理由、测试证据或历史记录；这与 task 必须短时间消失不是同一判断。
- `change-plan` 拥有可审阅和可交接的正式 change，`subagent-orchestration` 拥有代理配置与结果审计，两者都不拥有持续维护的通用任务状态。

## 决策

- 采用: `task-graph` 维护当前工作中仍有协调价值的任务发现、显式关系、状态查询、领取、完成和清理流程；经过时间本身不改变 task 的有效性，也不触发清理。
- 采用: `task-graph` skill 与兑现其行为所需的专用状态工具属于同一分发单元；工具提供确定性状态与拓扑操作，skill 负责触发、判断、权限、交接和完成条件。
- 采用: Task entry 只承接执行协调所需的任务内容、关系、状态、结果摘要和稳定引用；长期知识、正式 change 和历史证据继续由对应 owner 承接。
- 采用: 只有任务不再承担协调价值、结果已经交付且显式清理门禁满足时才删除；任务可以按实际需要跨上下文或长期保留。
- 采用: `task-graph` 不拥有代理创建、模型选择、上下文配置和结果审计；需要委派时把已就绪任务及其边界交给 `subagent-orchestration`。
