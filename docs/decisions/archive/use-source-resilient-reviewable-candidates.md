---
title: 使用可逐文件容错的显式完整候选
status: archived
alignment: aligned
createdAt: 2026-08-06T08:16:27Z
purpose: 让完整决策记录在正式建立前可等待审核、被可靠发现和直接删除，同时保持正式索引只表达已确认的决策集合。
background: 创建记录与确认其进入当前决策集合是两个动作；候选不进入索引后需要独立源码查询，而单条非法决策源码不应遮蔽其他可审核候选。
decision: 使用结构完整的显式 candidate 分离创建与审核；候选查询逐文件跳过非法源码，严格检查仍校验全集，审核后才选择对齐状态并进入正式索引。
tags:
  - decision-records
relations:
  - type: 修订
    target: use-explicit-reviewable-candidates.md
---

## 目的

- 让决策记录的创建、内容收敛与正式审核建立拥有清楚边界。
- 让尚未建立的候选可以跨任务保留、主动发现和直接删除，而不会冒充当前决策。
- 让单条非法源码不妨碍审核其他合法候选，同时保持集合级错误和严格验收可见。
- 保持正式索引只表达已经确认的决策集合，避免索引外候选成为不可见的悬空内容。

## 背景

- 一条记录可以已经写完整并值得审核，但是否进入当前决策集合、应当对齐还是未对齐，仍需要独立判断。
- 旧候选依赖 `status: active`、预先选择的 `alignment` 和空 `createdAt` 组合隐式识别，把尚未确认的记录提前表达成活动状态。
- 常规查询采用索引优先模型；候选不进入正式索引时，`list`、`show` 和 `trace` 无法主动发现它们。
- 源码查询如果因任意单条非法记录整体失败，该文件就会遮蔽其他合法候选，也会妨碍对问题文件逐条审核和处理。
- 候选可以等待审核，不应仅因其合法存在就让严格检查失败；结构不完整或状态非法的文件仍需要阻断最终验收。
- Git 提交只表达版本历史，不会把候选变成已经建立的决策；已提交候选不再需要时仍应可以删除。

## 决策

- 采用: 新候选使用稳定新身份、完整决策结构、`status: candidate`、`alignment: null` 和 `createdAt: null`；候选与已建立记录使用相同的摘要、正文和关系结构校验，不接受半成品。
- 采用: 候选尚未建立和生效，不进入正式索引，也不使用 Git `pending` 表达；`pending` 继续只属于版本快照。
- 采用: `candidates` 和 `show-candidate` 直接从 Markdown 源发现并读取合法候选；`list`、`show` 和 `trace` 继续只读取正式索引。
- 采用: 候选源码查询逐文件容错，跳过结构、状态或关系非法的单条源码并报告 warning；显式请求的目标自身非法时失败，决策根、根目录成员、领域目录表或领域目录布局非法时也整体失败。
- 采用: 严格 `check` 从源码发现并计数合法候选，候选未进入索引本身不构成失败；查询容错不改变验收门槛，不完整、状态组合非法、身份冲突或关系无效的记录继续阻断检查。
- 采用: `activate` 是审核与建立边界，为显式目标选择 `aligned` 或 `unaligned`、写入建立时间并加入正式索引；候选中的 `alignment` 不提前表达审核结论。
- 采用: 合法候选可以在审核前原地收敛或跨任务等待，不再需要且没有被引用时可以通过 `discard` 删除工作区文件；是否已经进入 Git `HEAD` 不影响候选删除，Git 历史和已建立记录不能用候选删除路径改写。
- 采用: 候选不作为正式关系目标或正式关系图成员；演进和拆分事务在建立后继时确认最终关系，并与前序生命周期和索引变化一起验证。
