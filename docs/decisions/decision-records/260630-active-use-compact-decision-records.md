# 2026-06-30 - 使用短日期命名并允许短决策结构

## 状态
- 当前状态: active
- 导致状态变化的决策: 无
- 状态说明: 作为当前决策文件日期前缀和短决策正文结构规则使用。

## 问题
- 决策文件名前缀使用完整日期时目录较长, 用户希望改成 `260627` 这类短日期。
- 现有正文模板要求每条决策都写背景与约束、决策过程等完整小节, 对命名、模板和维护偏好这类短决策不够友好。

## 决定
- 采用: 决策文件名使用 `YYMMDD-<status>-short-title.md`, 正文标题继续使用完整日期。
- 采用: 决策正文只强制 `## 状态`、`## 问题`、`## 决定`、`## 影响` 和 `## 验证`。
- 采用: `## 背景与约束` 和 `## 决策过程` 按需使用; 需要回放上下文、关键备选或多轮收敛时使用完整结构。
- 不采用: 为短决策省略状态关系或验证。原因是状态和验证分别承接有效性追踪与落地检查, 即使短决策也需要保留。
- 触发条件: 后续新增单点命名、模板、校验或维护偏好调整时, 可以使用短结构; 影响面较大或取舍复杂时使用完整结构。

## 影响
- 决策目录保持短日期文件名, 同时正文保留完整日期便于阅读。
- 小型决策可以避免填充低价值小节, 决策记录不再因为模板过重而接近变更日志。
- 决策校验需要允许可选小节, 但继续强制状态、问题、决定、影响和验证。

## 验证
- `skills/decision-records/references/decision-record-rules.md` 已写明短日期命名和短决策结构。
- `docs/decisions/decision-record-index.md` 已更新当前决策清单和状态关系。
- `scripts/validate-decisions.ts` 允许省略 `## 背景与约束` 和 `## 决策过程`, 并继续校验必需小节。
