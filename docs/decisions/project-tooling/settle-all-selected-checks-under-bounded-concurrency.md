---
title: 在有界并发下收敛全部已选检查
status: active
alignment: aligned
createdAt: 2026-08-11T04:14:56Z
purpose: 让已选择的前置检查在保守资源边界内全部形成结果，并在单项失败后继续发现其他独立问题。
background: 前置检查会启动 runner 和子进程；首个失败即停止会遗漏同一计划中的其他结果。
decision: 已选检查默认并发两个、允许正整数覆盖并优先长任务；单项失败不取消或停止其余已选检查。
relations:
  - type: 修订
    target: project-tooling/schedule-selected-checks-conservatively.md
---

## 目的

- 在不默认放大资源压力的前提下并行运行已经选择且可独立执行的前置检查。
- 让一次运行收集全部已选检查结果，而不是在首个失败后迫使维护者反复重跑。

## 背景

- 顶层检查可能各自启动测试 runner、临时仓库和多个子进程，并发数不能直接等同于机器核心数。
- 默认串行会浪费独立任务的等待时间，默认高并发则会制造不可预测的资源竞争。
- Full-only 集成任务通常明显更慢；若最后领取会延长保守并发下的总耗时。
- 已选前置检查彼此独立，某项失败只形成自己的结果，不构成取消其他已选项的依据。

## 决策

- 采用: 本次已选择的前置检查默认最多同时运行两个。
- 采用: 调用方可用 `CHECK_CONCURRENCY=<正整数>` 显式覆盖并发上限；非法值在任何前置检查启动前失败，不静默回退或推断。
- 采用: Full-only 长任务在已选任务中优先领取，以减少尾部等待；领取顺序不改变任务的 profile 或验证范围。
- 采用: 任一检查失败后记录该结果，但工作线程继续领取尚未运行的已选检查；已经运行的检查也不中止，直到全部已选前置检查形成结果。
- 采用: 本记录的输出止于完整前置检查结果集合，不决定结果展示、最终退出状态或后续打包门禁。
