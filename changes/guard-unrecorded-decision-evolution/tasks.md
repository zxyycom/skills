# Tasks

任务先建立长期契约，再实现预检与折叠事务，完成出口是行为、分发制品和测试证据同时通过严格检查。

## Readiness

- [x] 0.1 核对现行 Markdown 生命周期、演进事务、`discard` 边界、共享版本管理接口和原调查，确认 Git 只作为预检证据。
- [x] 0.2 确认采用当前 `HEAD`、首次警告不写入、显式保留或单前序折叠，并将复杂关系保留给人工判断。
- [x] 0.3 确认 `Open Questions` 为“无”，用户已授权按扩展 `evolve` 和归档预警的方向实施。

## Implementation

- [x] 1.1 新增 Git `HEAD` 决策路径适配和可区分的“命令已暂停”应用结果，不改变非 Git 生命周期。
- [x] 1.2 为 `activate`、`evolve` 和 `archive` 增加显式保留参数，并在事务前拦截未进入 `HEAD` 的归档目标。
- [x] 1.3 扩展 `evolve` 支持折叠一个未提交活动前序、完整最终关系和引用/上游边界校验，复用可恢复事务删除记录并重建索引。
- [x] 1.4 更新 decision-records 行为入口、领域契约、人类说明和 skill 版本，并从工具源码同步分发制品。
- [x] 1.5 建立并在实施完成后对齐本 change 的长期决策，追加并结束同一变更内决策演进调查。

## Verification

- [x] 2.1 分别覆盖首次警告无写入、显式保留、带关系和空关系折叠、非法上游、已进入 `HEAD`、其他引用、删除后回滚，以及非 Git 场景不增加调用。
- [x] 2.2 为每个新增或修改的最小原生测试入口维护独立测试证据 case，并同步统一派生索引。
- [x] 2.3 运行 decision-records 目标测试、生成漂移检查、严格决策与调查检查、类型检查和 `bun run check`。
- [x] 2.4 审阅最终 diff，确认没有放宽 `discard`、没有让 Git 拥有建立状态，也没有遗留未激活候选或未完成计划任务。
