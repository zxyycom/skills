# Tasks

任务以共享 segment matcher 支撑两个领域的纯索引 metadata 搜索；所有 checkbox 仅在对应实现和验证证据实际完成后勾选。

## Readiness
- [x] 0.1 在实施开始时重新读取 `skills/decision-records/SKILL.md`、`skills/investigation-report/SKILL.md`、`docs/tooling.md`、`docs/coding-style.md` 及本 Plan，审计 `search` 的 CLI/API 调用方、结构过滤、确定顺序、错误输出和实际生成入口；确认本 Change 不与 `remove-index-runtime-text-mode` 交叠。
- [x] 0.2 为当前 content 搜索建立兼容基线：Decision 的 active/archived、alignment/tag、按 sourcePath 遍历的 preview `maxFiles` 截断和只读 source fallback；Investigation 的 tag/formedAt/relation-type、唯一 `--limit`（默认 50、最大 1000）、按 sourcePath 遍历的 preview `maxFiles` 截断和只读 source fallback。记录现有物理行 `all`/`any`/`phrase` 语义，并确认两个 search 都没有 `--offset`、`total` 或记录分页，避免 metadata 实现改变它。
- [x] 0.3 relation summary 已作为可选的已发布索引投影可供消费。本 Change 只读取该投影，绝不修改 relation source/parser/writeback、graph/lifecycle、schema/definitionVersion/sourceRevision、rename 或旧索引迁移。
- [x] 0.4 列出本 Change 新增或修改的最小原生测试入口及其现有 case；在 `decision-records` 与 `investigation-report` topic 选择或新建一入口一 case，明确 `Entry:`、`Contract:`、`Proves:` 和实际 runner 选择方式，避免把 shared helper 或聚合 runner 单独登记。

## Implementation
- [x] 1.1 在 `tools/shared/src/file-text-search/` 抽取最小纯 query/segment matcher：保留 NFKC、大小写和空白归一化、原文范围、all/any/phrase 与 abort 边界；使文件搜索以物理行 segment 调用它，保持现有文件路径验证、UTF-8 读取、预览和 truncation 公共 API 不变。
- [x] 1.2 扩展 Decision CLI 参数、request/result 类型、查询服务与文本输出，支持默认 `--in content` 和显式 `--in metadata`；metadata branch 只加载持久 Decision index、先复用 status/alignment/tag 过滤，再以 `id`/`name`/projection/tags 和已发布的非空 relation summary segments 匹配，按既有确定顺序返回全部来源记录、稳定顺序 `matchedFields` 与实际命中、且仅属于该来源记录的 summary 的 `matchedRelations(type,target,summary)`。不新增 `--limit`、`--offset`、`total`、分页、`--in relations` 或 content 的 preview 限额；索引错误以 check/sync-index recovery 失败，不读取实体或反查 target。
- [x] 1.3 扩展 Investigation CLI token/options、程序化 search options/result、查询服务与文本输出，支持默认 `--in content` 和显式 `--in metadata`；metadata branch 只加载持久 Investigation index、先复用 tag/formedAt/relation-type 过滤，再以 `id`/`name`/title/question/tags 和已发布的非空 relation summary segments 匹配，按既有确定顺序形成完整来源记录集合后应用唯一现有 `--limit`（默认 50、最大 1000），并输出 `matchedFields` 与仅属于该来源记录的 `matchedRelations(type,target,summary)`。不新增 `--offset`、`total`、分页、`--in relations` 或 content 的 preview 限额；索引错误直接失败且不扫描报告、candidate、资源或 target。
- [x] 1.4 为两个领域补充覆盖字段白名单、跨字段/summary `all`、单字段/单 tag/单 relation-summary `phrase`、`any`、NFKC/空白/大小写、matchedFields/matchedRelations 的值与顺序、结构条件先行、空结果和 metadata 索引失败的测试；证明前置投影中的缺失/空 summary、type、target 和 target 记录本身不会命中，且 metadata 成功和失败路径都不读取实体，而 content fallback 仍可读取实体。
- [x] 1.5 更新 `skills/decision-records/SKILL.md` 与 `skills/investigation-report/SKILL.md` 的 search 指导：区分 `content`/`metadata`、已发布 summary 的来源归属、字段与 `matchedRelations` 输出、phrase segment 边界、snapshot 滞后及 `check`/`sync-index` recovery；不把 relation summary 的来源/迁移规则、Change 背景或 Test Evidence 迁移过程复制进 search owner。
- [x] 1.6 按 `skills/test-evidence-review/` 为 0.4 列出的每个保留测试入口写入或更新唯一 case，并通过 `bun run test-evidence -- sync-index --write` 重建派生索引；若提交只选择部分 case，额外按契约核对 selected sync/stage，不手工编辑索引。
- [x] 1.7 从维护源码运行 `bun run sync:decision-records-cli` 与 `bun run sync:investigation-report-check`，提交本 Change 实际受影响的 query bundle、source map 和声明：Decision 的 `decision-records.mjs`、map、入口 `.d.mts` 与 `decision-records-sdk/`，以及 Investigation 的 `check-investigations.mjs`、map 与入口 `.d.mts`。不得直接编辑生成文件或借此修改 Index Runtime/Test Evidence 的 `text` protocol。

## Verification
- [x] 2.1 运行 `bun test ./tools/shared/tests/file-text-search.test.ts`，证明重构后文件 `all` 可跨物理行、`phrase` 不跨物理行，且 Unicode/NFKC、空白、范围、preview 与资源/路径失败边界保持既有契约；为纯 segment matcher 增加相应独立断言。
- [x] 2.2 运行 `bun run test:decision-records-cli`，并在 fixture root 实际调用 `bun run decision-records -- search <text> --in content --root <fixture-root>` 与 `bun run decision-records -- search <text> --in metadata --root <fixture-root>`；验证省略 `--in` 的 content 兼容、metadata 返回来源 ID/摘要/sourcePath/matchedFields/matchedRelations 而不返回 previews、relation summary 的 source-only 命中、空 summary/type/target/target record 不命中、索引缺失/损坏/definition 错误非零且提示 `check`/`sync-index`，以及 metadata 不读取实体的证明。
- [x] 2.3 运行 `bun run test:investigation-report-check`，并在 fixture root 实际调用 `bun run investigation-report -- search <text> --in content --root <fixture-root>` 与 `bun run investigation-report -- search <text> --in metadata --root <fixture-root>`；验证相同范围默认、metadata matchedFields/matchedRelations、relation summary source-only 命中、空 summary/type/target/target record 不命中、既有结构过滤、先形成完整确定顺序再应用 `--limit`（默认 50、最大 1000）、候选和资源排除、无实体读取及索引错误失败，确认没有 offset/total/分页，且 content 分支仍按 sourcePath 遍历、受 preview 截断并保留只读 fallback。
- [x] 2.4 在 source/index fixtures 和 `sync-index` 中确认 metadata 只消费 summary projection 而不改写它；运行 `bun run check:decision-records-cli`、`bun run check:investigation-report-check`、`bun run check:test-evidence-catalog` 与 `bun run check:test-evidence-cli`，证明 query bundle/source map/声明与测试证据索引均无漂移。
- [x] 2.5 运行 `bun run check`；人工复核 public help、两个 skill 的 search 指导、metadata output 和测试证据，确认没有新增 `searchText`、通用领域搜索框架、实体 fallback、target 反向搜索、`--in relations`、relation summary 写入/Schema/迁移改动，或 Test Evidence/Index Runtime text-mode 改动。
