interface:
  display_name: "Test Evidence Review"
  short_description: "评估测试契约与证明价值，同步入口级审计记录"
  default_prompt: "使用 $test-evidence-review 评估当前测试或稳定风险，先恢复文字契约和既有证据，再选择保留、复用、合并、自动化、人工审查或豁免，并在授权范围内同步账本、入口映射、Scope trigger 状态和必要验证。"
