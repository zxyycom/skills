interface:
  display_name: "Test Evidence Review"
  short_description: "评估测试契约与证明价值，同步入口级审计记录"
  default_prompt: "使用 $test-evidence-review 评估当前测试或稳定风险，先从项目 owner 和账本查询恢复文字契约、既有证据、入口映射与 trigger，再选择保留、复用、合并、自动化、人工审查或豁免；入口采集默认使用常见规则，复杂项目通过配置或标准清单接入，并在授权范围内同步记录和验证。"
