interface:
  display_name: "Git 提交整理"
  short_description: "按固定格式审计差异并整理创建 Git 提交信息"
  default_prompt: "Use $git-commit-organizer to review the current diff, choose the commit type, stage relevant files, and create the commit."
