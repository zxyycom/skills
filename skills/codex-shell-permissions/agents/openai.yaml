interface:
  display_name: "Shell 使用与权限"
  short_description: "shell 失败分类、提权边界与权限 rules"
  default_prompt: "使用 $codex-shell-permissions 根据实际执行身份、原始命令、失败阶段和风险边界选择一个下一步；只有我明确要求时才维护长期 permission rules。"
